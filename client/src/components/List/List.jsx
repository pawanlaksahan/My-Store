import React from 'react'
import { Card } from '../Card/Card';
import './List.scss'

export const List = () => {

     

  return (
    <div className="list">
        {/* {data.map(value=>(
            <Card value={value} key={value.id}/>
        ))} */}

           
        
    </div>
  )
}
